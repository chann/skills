---
description: Summarize explicitly selected plan documents in Korean and English Markdown plus one bilingual HTML report.
argument-hint: "[source-path ...]"
---

Apply the **plan-summary** skill internally. Do not echo this routing instruction.

Treat every item in `$ARGUMENTS` as explicit path data. Do not interpolate it into a shell command, expand globs, or discover substitute documents. If no source path was supplied, ask for one or more explicit files.

Follow the packaged collector, evidence-first analysis, aligned Korean and English report, and generator contracts. Complete only after writing both Markdown files and one self-contained bilingual HTML file, validating them, and making a browser-open attempt.

Report artifact and verification facts only. Do not repeat the generated summary prose in the conversation.
