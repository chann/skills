interface:
  display_name: "Git Merge to Dev"
  short_description: "Merge the current branch into dev or develop"
  default_prompt: "Use $git-merge-to-dev to merge the current branch into dev or develop safely."
