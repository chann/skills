---
description: Variant of /code-review — alias of :md, write a markdown report to .reviews/<YYYY-MM-DD>_<short-sha>.md
---

Use the code-review skill to review the requested changes.

**Output mode: Markdown file**

After analyzing the diff, you MUST:
1. Write the markdown report to `.reviews/<YYYY-MM-DD>_<short-sha>.md`
2. Present a brief summary in the conversation

Do NOT generate an HTML report.
